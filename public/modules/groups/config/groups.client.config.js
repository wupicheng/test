'use strict';

// Configuring the Articles module
angular.module('groups').run(['Menus',
	function(Menus) {
		// Set top bar menu items
	//	Menus.addMenuItem('topbar', 'Articles', 'groups', 'dropdown', '/groups(/create)?');
	//	Menus.addSubMenuItem('topbar', 'groups', 'List Articles', 'groups');
	//	Menus.addSubMenuItem('topbar', 'groups', 'New Article', 'groups/create');
	}
]);