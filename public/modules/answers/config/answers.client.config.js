'use strict';

// Configuring the Articles module
angular.module('answers').run(['Menus',
	function(Menus) {
		// Set top bar menu items
	//	Menus.addMenuItem('topbar', 'Articles', 'answers', 'dropdown', '/answers(/create)?');
	//	Menus.addSubMenuItem('topbar', 'answers', 'List Articles', 'answers');
	//	Menus.addSubMenuItem('topbar', 'answers', 'New Article', 'answers/create');
	}
]);